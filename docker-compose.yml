services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=postgresql://postgres:hola1234T@db:5432/PostgresIn10
    depends_on:
      - db

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: hola1234T
      POSTGRES_DB: PostgresIn10
    ports:
      - "5432:5432"
